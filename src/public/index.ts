// This syntax is made possible by the module 'babel-plugin-file-loader'
// '@ts-ignore' ignores the unfortunate linting errors

// @ts-ignore
import thumbsup from './icons/thumbsup.svg';

// @ts-ignore
import check from './icons/check.svg';

// @ts-ignore
import expressByCurrency from './express-by-currency.png';

const imagePaths: any = {
  thumbsup,
  check,
  expressByCurrency,
};

export default imagePaths;
